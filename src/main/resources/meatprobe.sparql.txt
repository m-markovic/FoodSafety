REGISTER QUERY meatteamp AS
PREFIX fs:<http://foodsafety-onto#>
SELECT (MIN(?time) AS ?mintime) (MAX(?time) AS ?maxtime) (MAX(?value) AS ?maxtemp)
FROM STREAM <http://foodsafety/parsed> [RANGE 1m TUMBLING]
WHERE {
  fs:meatProbe fs:observes ?obs.
  ?obs fs:value ?value.
  ?obs fs:time ?time
}
